# Archivio delle sonde (pulizia del 14/09/2026)

Qui stanno le sonde di misura scritte fino ad agosto 2026 che nessuno script di `package.json`, nessun
check, nessuna libreria e nessun workflow richiama piu' (310 file + `scratch/`). Restano nella storia e qui,
leggibili, perche' i verbali in `docs/FASE3-SCAMBIO.md` e `docs/RELEASE_HISTORY.md` le citano per nome.
Gli import sono relativi alla cartella di origine (`./lib/harness.mjs`): per rieseguirne una, riportala in
`tests/visual/` con `git mv`. Le sonde vive (settembre 2026 e quelle richiamate dagli script) stanno sopra.
